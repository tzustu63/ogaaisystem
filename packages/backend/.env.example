# OGA AI System 環境變數範本
# 複製此檔案為 .env 並填入實際值
# 警告：以下所有標記為 [必填] 的項目在生產環境中都必須設定

# ===== 資料庫設定 [必填] =====
DB_HOST=localhost
DB_PORT=15432
DB_NAME=oga_ai_system
DB_USER=postgres
# 使用強密碼，生產環境必須設定
DB_PASSWORD=your-secure-database-password

# ===== Redis 設定 =====
REDIS_HOST=localhost
REDIS_PORT=16379
# 可選：Redis 密碼（如果 Redis 啟用了認證）
REDIS_PASSWORD=

# ===== MinIO 物件儲存設定 [必填] =====
MINIO_ENDPOINT=localhost
MINIO_PORT=19000
MINIO_USE_SSL=false
# MinIO 存取金鑰
MINIO_ACCESS_KEY=your-minio-access-key
# MinIO 密鑰
MINIO_SECRET_KEY=your-minio-secret-key
MINIO_BUCKET=oga-ai-system
# 可選：MinIO 公開存取 URL
MINIO_PUBLIC_URL=

# ===== JWT 認證設定 [必填] =====
# 使用以下命令生成安全的 Secret：
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 必須至少 32 個字元
JWT_SECRET=your-jwt-secret-key-at-least-32-characters-long
# Token 過期時間
JWT_EXPIRES_IN=24h

# ===== Server 設定 =====
PORT=3001
# development | production | test
NODE_ENV=development

# ===== CORS 設定 =====
# 逗號分隔的允許來源（可選，會合併到預設允許列表）
CORS_ORIGINS=

# ===== Google AI 設定 =====
# Google Gemini API 金鑰（AI 小幫手功能需要）
GOOGLE_AI_API_KEY=your-google-ai-api-key

# ===== SMTP 郵件設定 =====
# 如需啟用郵件通知功能，請設定以下項目
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASSWORD=your-smtp-password

# ===== LINE Notify 設定 =====
# 如需啟用 LINE 通知功能，請設定以下項目
LINE_NOTIFY_TOKEN=your-line-notify-token
